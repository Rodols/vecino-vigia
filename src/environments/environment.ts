// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBGt9YgLswjq_5lkr18NbBDjKYE6H9d_d0',
    authDomain: 'vecinosvigia.firebaseapp.com',
    databaseURL: 'https://vecinosvigia.firebaseio.com',
    projectId: 'vecinosvigia',
    storageBucket: '',
    messagingSenderId: '747234463961',
    appId: '1:747234463961:web:c44ae977a0c9f019'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
